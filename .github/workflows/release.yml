name: Release Obsidian Plugin

on:
  push:
    # Sequence of patterns matched against refs/tags
    tags:
      - "*" # Push events to matching any tag format, i.e. 1.0, 20.15.10

permissions:
  contents: write # to create a release and upload assets

env:
  OUT_DIR: dist # ${{ github.event.repository.name }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: 'recursive'
          fetch-depth: 0

      # Get the version number and put it in a variable
      - name: Get Version
        id: version
        run: |
          echo "::set-output name=tag::$(git describe --abbrev=0 --tags)"

      # Build the plugin
      - name: Use bun instead of Node.js
        uses: oven-sh/setup-bun@v2
      - name: Build (bun)
        id: build
        run: |
          bun install
          bun run build

      # Package the required files into a zip
      # skip existing dir for mkdir
      # don't fail cp on optional files like styles.css
      # main.js already in output-dir
      - name: Package
        run: |
          mkdir -p ${{ env.OUT_DIR }}
          cp manifest.json ${{ env.OUT_DIR }}
          cp styles.css ${{ env.OUT_DIR }} 2>/dev/null || :
          cp README.md ${{ env.OUT_DIR }} 2>/dev/null || :
          zip -r ${{ github.event.repository.name }}.zip ${{ env.OUT_DIR }}
          mv ${{ github.event.repository.name }}.zip ${{ env.OUT_DIR }}/${{ github.event.repository.name }}-${{ steps.version.outputs.tag }}.zip

      - name: Release with assets
        uses: softprops/action-gh-release@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ github.ref }}
        with:
          tag_name: ${{ github.ref }}
          # name: ${{ github.ref }}
          draft: true
          prerelease: false
          working_directory: ${{ env.OUT_DIR }}
          files: |
            *.*
          generate_release_notes: true
